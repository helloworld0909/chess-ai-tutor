{
  "_comment": "DeepSpeed ZeRO-2 for QLoRA + 2-GPU training. ZeRO-2 shards optimizer states and gradients across GPUs without sharding model weights, which is compatible with bitsandbytes quantization. ZeRO-3 is NOT recommended with QLoRA.",
  "zero_optimization": {
    "stage": 2,
    "allgather_partitions": true,
    "reduce_scatter": true,
    "overlap_comm": true,
    "contiguous_gradients": true
  },
  "bf16": {
    "enabled": true
  },
  "gradient_clipping": 1.0,
  "train_micro_batch_size_per_gpu": "auto",
  "gradient_accumulation_steps": "auto",
  "steps_per_print": 10,
  "wall_clock_breakdown": false
}
